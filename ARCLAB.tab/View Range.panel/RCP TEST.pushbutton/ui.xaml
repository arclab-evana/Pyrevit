<Grid Grid.Row="2" Margin="0,10,0,0">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Button x:Name="UpButton" Content="▲" Grid.Column="0" Height="35" Margin="2" 
            Background="#F5F5F5" FontWeight="Bold" FontSize="12"/>
    <Button x:Name="DownButton" Content="▼" Grid.Column="1" Height="35" Margin="2" 
            Background="#F5F5F5" FontWeight="Bold" FontSize="12"/>
    
    <Button x:Name="ResetButton" Grid.Row="1" Grid.ColumnSpan="2" Content="Reset RCP" 
            Height="25" Margin="2,5,2,0" Background="White" BorderBrush="Black" 
            BorderThickness="1" Foreground="Black" FontSize="10" FontWeight="Bold"/>
</Grid>